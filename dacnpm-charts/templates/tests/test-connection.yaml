apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dacnpm-charts.fullname" . }}-test-connection"
  labels:
    {{- include "dacnpm-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dacnpm-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
